@extends('layouts.app') @section('title','Administrator Login | NexPort') @section('content')<section class="login-section"><div class="login-card"><div class="login-brand"><span class="logo-mark">&#9875;</span><div><b>NEXPORT</b><small>SECURE ADMINISTRATION</small></div></div><span class="eyebrow" style="color:#b98a27">Restricted access</span><h1>Welcome back</h1><p class="login-intro">Sign in with your administrator account to manage products and website operations.</p>@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('admin.login.submit') }}">@csrf<div class="field"><label for="email">Email address</label><input id="email" type="email" name="email" value="{{ old('email') }}" autocomplete="email" required autofocus></div><div class="field"><label for="password">Password</label><input id="password" type="password" name="password" autocomplete="current-password" required></div><label class="remember"><input type="checkbox" name="remember" value="1"> Keep me signed in on this device</label><button class="button gold login-button" type="submit">Sign in securely</button></form><p class="login-security">Protected by Laravel session authentication, CSRF protection and login rate limiting.</p></div></section>@endsection
