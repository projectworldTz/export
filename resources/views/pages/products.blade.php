@extends('layouts.app') @section('title','Product Catalogue | NexPort') @section('content')<x-page-head tag="Global catalogue" title="Products sourced for progress" text="Verified quality, transparent pricing and international delivery support."/><section class="section cream"><div class="container"><form class="filters" method="get"><input name="search" value="{{ request('search') }}" placeholder="Search products or brands"><select name="category"><option value="">All categories</option>@foreach(['Vehicles','Motorcycles','Construction Materials','Machinery','Electronics','Spare Parts'] as $c)<option @selected(request('category')===$c)>{{ $c }}</option>@endforeach</select><select name="sort"><option value="latest">Latest</option><option value="price_asc" @selected(request('sort')==='price_asc')>Price low to high</option><option value="price_desc" @selected(request('sort')==='price_desc')>Price high to low</option></select><button class="button">Apply filters</button></form>@if($products->count())<div class="grid grid-4">@foreach($products as $product)<x-product-card :product="$product"/>@endforeach</div><div class="pagination">{{ $products->links() }}</div>@else<div class="form-card" style="text-align:center"><h2>No products found</h2><p>Change your filters or send us a custom sourcing request.</p><a class="button" href="{{ route('imports.create') }}">Custom import request</a></div>@endif</div></section>@endsection
