@extends('layouts.app') @section('title','Branding Settings') @section('content')<div class="admin-toolbar"><a href="{{ route('admin') }}">Dashboard</a><a href="{{ route('admin.products.index') }}">Products</a><a href="{{ route('admin.branding.edit') }}">Branding</a><span>{{ auth()->user()->name }}</span><form method="post" action="{{ route('admin.logout') }}">@csrf<button>Sign Out</button></form></div><section class="section cream"><div class="container" style="max-width:820px"><div class="between"><div><span class="eyebrow" style="color:#b98a27">Website Content</span><h1>Logo & Branding</h1><p class="help">Upload one logo and it will appear automatically across the public website.</p></div><a class="button outline" href="{{ route('admin') }}">Dashboard</a></div>@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form class="form-card branding-form" method="post" enctype="multipart/form-data" action="{{ route('admin.branding.update') }}">@csrf @method('put')<div class="branding-preview"><small>Current public logo</small>@if($logo)<img src="{{ asset($logo) }}" alt="Current logo">@else<div class="fallback-preview"><span class="logo-mark">&#9875;</span><b>{{ $siteName }}</b></div>@endif</div><div class="form-grid"><div class="field"><label>Company name</label><input name="site_name" value="{{ old('site_name',$siteName) }}" required></div><div class="field"><label>Tagline</label><input name="site_tagline" value="{{ old('site_tagline',$tagline) }}"></div><div class="field full"><label>Upload new logo</label><input type="file" name="logo" accept="image/jpeg,image/png,image/webp"><small class="help">PNG or WebP with transparent background recommended. Maximum 2 MB.</small></div>@if($logo)<label class="field full remove-logo"><input type="checkbox" name="remove_logo" value="1"> Remove uploaded logo and restore the default NexPort mark</label>@endif</div><button class="button gold" style="width:100%">Save Branding</button></form></div></section>@endsection
