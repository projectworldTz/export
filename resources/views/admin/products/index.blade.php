@extends('layouts.app') @section('title','Manage Products') @section('content')<section class="section cream"><div class="container"><div class="between"><div><span class="eyebrow" style="color:#b98a27">Administration</span><h1>Product Management</h1></div><a class="button gold" href="{{ route('admin.products.create') }}">+ Add Product</a></div>@if(session('success'))<div class="alert success" style="margin-top:25px">{{ session('success') }}</div>@endif<div class="admin-card" style="margin-top:25px;overflow:auto"><table class="table"><thead><tr><th>Product</th><th>Category</th><th>Stock</th><th>Price</th><th>Mode</th><th>Images</th><th>Actions</th></tr></thead><tbody>@foreach($products as $product)<tr><td><div style="display:flex;align-items:center;gap:12px"><img src="{{ $product->primary_image }}" style="width:65px;height:48px;object-fit:cover;border-radius:7px"><b>{{ $product->name }}</b></div></td><td>{{ $product->category }}</td><td>{{ $product->stock_no }}</td><td>{{ $product->currency }} {{ number_format($product->price) }}</td><td>{{ ucfirst($product->purchase_mode) }}</td><td>{{ $product->images_count }}</td><td style="white-space:nowrap"><a class="button small" href="{{ route('products.show',$product) }}">View</a> <a class="button small gold" href="{{ route('admin.products.edit',$product) }}">Edit</a> <form method="post" action="{{ route('admin.products.destroy',$product) }}" style="display:inline" onsubmit="return confirm('Remove this product?')">@csrf @method('delete')<button class="button small danger">Delete</button></form></td></tr>@endforeach</tbody></table></div>{{ $products->links() }}</div></section>@endsection
