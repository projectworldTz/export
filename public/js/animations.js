(()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -45px'});document.querySelectorAll('section,.product-card,.category,.step,.admin-card,.shipping-card,.form-card').forEach((el,index)=>{el.dataset.reveal='';el.style.setProperty('--reveal-delay',`${Math.min(index%4,3)*90}ms`);observer.observe(el)});const hero=document.querySelector('.hero-content');if(hero)hero.classList.add('hero-animate')}document.querySelectorAll('.stats b').forEach(counter=>{const match=counter.textContent.trim().match(/([0-9]+)(.*)/);if(!match)return;const target=Number(match[1]),suffix=match[2];let started=false;const run=()=>{if(started||reduced)return;started=true;const start=performance.now(),frame=now=>{const p=Math.min((now-start)/1200,1);counter.textContent=`${Math.round(target*(1-Math.pow(1-p,3)))}${suffix}`;if(p<1)requestAnimationFrame(frame)};requestAnimationFrame(frame)};new IntersectionObserver(entries=>entries[0].isIntersecting&&run(),{threshold:.5}).observe(counter)});const nav=document.querySelector('.nav');addEventListener('scroll',()=>nav?.classList.toggle('nav-scrolled',scrollY>30),{passive:true});document.querySelectorAll('.button').forEach(button=>button.addEventListener('pointerdown',event=>{const ripple=document.createElement('span'),rect=button.getBoundingClientRect();ripple.className='ripple';ripple.style.left=`${event.clientX-rect.left}px`;ripple.style.top=`${event.clientY-rect.top}px`;button.appendChild(ripple);setTimeout(()=>ripple.remove(),650)}));document.documentElement.classList.add('animations-ready')})();
